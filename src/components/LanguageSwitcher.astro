---
import strings from '../i18n/strings.json';

interface Props {
  locale: string;
}

const { locale } = Astro.props;
const pathname = Astro.url.pathname;

const enPath = locale === 'ar' ? (pathname.replace(/^\/ar/, '') || '/') : pathname;
const arPath = locale === 'ar' ? pathname : ('/ar' + (pathname === '/' ? '/' : pathname));

const s = strings[locale as 'en' | 'ar'].switcher;
---

<div class="flex items-center gap-1.5 text-sm">
  <a
    href={enPath}
    class={locale === 'en' ? 'text-text-primary underline underline-offset-2' : 'text-text-tertiary hover:text-text-primary transition-colors duration-200'}
  >{s.en}</a>
  <span class="text-text-quaternary">/</span>
  <a
    href={arPath}
    class={locale === 'ar' ? 'text-text-primary underline underline-offset-2' : 'text-text-tertiary hover:text-text-primary transition-colors duration-200'}
  >{s.ar}</a>
</div>
